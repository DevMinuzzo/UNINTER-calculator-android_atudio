<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="btn_container">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">28sp</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <style name="btn_operation" parent="btn_container">
        <item name="backgroundTint">@color/operators</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="btn_equals" parent="btn_container">
        <item name="backgroundTint">@color/equals</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="btn_number" parent="btn_container">
        <item name="android:backgroundTint">@color/dark_gray</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="display_container">
        <item name="android:background">@color/display_container</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="display">
        <item name="android:background">@color/display</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:gravity">center_vertical|right</item>
        <item name="android:ellipsize">start</item>
    </style>

</resources>